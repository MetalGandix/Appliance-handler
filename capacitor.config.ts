import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'it.ubisive.vismap',
  appName: 'Vismap',
  webDir: 'dist/appliance-handler',
  bundledWebRuntime: false
};

export default config;
